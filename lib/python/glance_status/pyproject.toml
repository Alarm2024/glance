[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "glance_status"
version = "0.0.0"
description = "Thin Python mirror of glance-status — doctor status, redaction, anti-overclaim"
license = { text = "MIT" }
requires-python = ">=3.9"

[project.optional-dependencies]
dev = ["pytest>=7"]

[tool.setuptools.packages.find]
where = ["."]
include = ["glance_status*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
