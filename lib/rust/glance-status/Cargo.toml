[package]
name = "glance-status"
version = "0.1.0"
edition = "2021"
description = "Fault-first doctor status, redaction, and anti-overclaim checks for Glance"
license = "MIT"
readme = "../../../README.md"

[dependencies]
regex = "1.10"

[dev-dependencies]
